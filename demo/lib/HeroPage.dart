import 'package:flutter/material.dart';

class Heropage extends StatelessWidget {
  const Heropage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(height: 200, width: 200, color: Colors.red));
  }
}
